<div class="container">
	<div class="row">
		<% @windows.each do |window| %>
		<div class="col-md-4">
			<%= image_tag window.image.url(:medium)%>
			<h3><%= "#{window.store_name} | #{window.location}" %></h3>
		</div>
		<% end %>
	</div>
</div>